import React from 'react';
import PropTypes from 'prop-types';

const InfoBar = ({/*timer, totalScore, currentStreakBonus*/}) => {
    return (
        <div>
            <div>Timer: </div>
            <div>Total Score: </div>
            <div>Current Streak Bonus: </div>
        </div>
    );
};

// InfoBar.propTypes = {
//     timer: ,
    // totalScore: ,
    // currentStreakBonus:
// };

export default InfoBar;
